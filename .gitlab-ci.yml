stages:
  - test
  - build

test:
  stage: test
  image: python:3.12
  script:
    - python --version
    - pip install -r requirements.txt
    - python manage.py test

build:
  stage: build
  image: docker:latest
  script:
    - docker version
    - docker build -t taxitracker .